language: bash
install:
  - git symbolic-ref --short HEAD || git checkout ${TRAVIS_BRANCH}
script:
  - |
    if [ "$TRAVIS_PULL_REQUEST" = "false"]
    then
      ./bs publish --publish --buildNum=${TRAVIS_BUILD_NUMBER}
    else
      ./bs publish --buildNum=${TRAVIS_BUILD_NUMBER}
    fi
